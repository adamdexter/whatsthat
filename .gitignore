# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Node
node_modules/
npm-debug.log*

# WhatsApp session data (auth credentials — never commit)
.wwebjs_auth/
.wwebjs_cache/

# Local run artifacts
reports/
*.local.json
*.log
.env

# Patches retired by the launch-time auto-updater (restorable, see CLAUDE.md)
patches-retired/
dist/
build/icon.iconset/
